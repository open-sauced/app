import Repositories from "components/organisms/Repositories/repositories";

const StoryConfig = {
  title: "Design System/organisms/Repositories"
};

export default StoryConfig;

export const ReportsOrganism = (): JSX.Element => <Repositories />;
